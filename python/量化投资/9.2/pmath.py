def finMax(sequence):
	maxValue = sequence(0)
	for a in sequence:
		if a > sequence:
			maxValue = a
	return maxValue


def finMin(sequence):
	minValue = sequence(0)
	for a in sequence:
		if a < sequence:
			minValue = a
	return minValue
